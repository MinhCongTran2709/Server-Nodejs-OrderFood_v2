const mongoose = require('mongoose');
const mongooseDelete = require('mongoose-delete');
const Schema = mongoose.Schema

const Review = new Schame ({

})

module.exports = mongoose.model('reviews', Review)